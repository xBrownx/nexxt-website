import * as React from 'react';import { historyAssets as ASSETS } from '@/assets';import { SectionTitle } from '@/components/layouts';import Video from '@/components/ui/video/video';import { useNavigationContext } from '@/context/navigation-context';import { useMobile } from '@/hooks/use-mobile';export const HistoryRoute: React.FC = () => {    const { pageRefs } = useNavigationContext();    const isMobile = useMobile();    return (        <section            ref={pageRefs.history}            className={`h-fit w-screen bg-background-primary`}        >            <div                className={`box-border flex size-full h-fit flex-row items-center justify-center overflow-hidden pt-16  md:gap-32 md:px-40 md:pb-10 md:pt-32`}            >                <SectionTitle                    variant={isMobile ? 'mobile' : 'row'}                    size={isMobile ? 'default' : 'row'}                    indentVariant={isMobile ? 'default' : 'flexEnd'}                    subtitle={'HISTORY'}                    title={'The Legacy of 140 William Street'}                    paragraph={                        'Ideally located in the heart of Melbourne CBD,' +                        'enjoying a prime position on the corner of Bourke street' +                        'and William Street.'                    }                />            </div>            <div                className={`box-border flex h-fit justify-center overflow-hidden rounded-lg  px-4 pb-16 md:px-16 md:pb-32`}            >                <div                    className={`w-full overflow-hidden rounded-lg md:w-[70rem]`}                >                    <Video                        options={{                            src: ASSETS.vid['hero-video'].src,                            controls: true,                            autoplay: false,                            muted: false,                            poster: ASSETS.img['video-poster'].src,                        }}                    />                </div>            </div>        </section>    );};