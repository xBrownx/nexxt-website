import * as React from 'react';import { FadeSection } from '@/components/layouts';import { useNavigationContext } from '@/context/navigation-context';export function LandingOverview() {    const { pageRefs } = useNavigationContext();    return (        <FadeSection initColor={'#c4adee'} enterColor={'#FFFFFF'}>            <div className="flex h-screen w-full items-center justify-center">                <h1 className={'text-8xl text-accent-purple'}>HELLO</h1>            </div>        </FadeSection>    );}