import { useState } from 'react';import { THEME_COLOURS } from '@/constants/theme-colours';import { HomeLanding, HomeOverview } from '@/routes/landing/pages';const HomeRoute = () => {    const [bgColor, setBgColor] = useState<string>(THEME_COLOURS.accent.purple);    return (        <div            className={'relative transition-all duration-1500'}            style={{ backgroundColor: bgColor }}        >            <HomeLanding />            <HomeOverview setBgColour={setBgColor} />        </div>    );};export default HomeRoute;