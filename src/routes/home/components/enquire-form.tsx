import React from 'react';import { Input } from '@/components/ui/input';import { cn } from '@/utils/cn';interface FormState {    firstName: string;    lastName: string;    email: string;    phone: string;    message: string;}export const EnquireForm = () => {    const [inputs, setInputs] = React.useState<FormState>({        email: '',        firstName: '',        lastName: '',        message: '',        phone: '',    });    const handleChange = (event: any) => {        const name = event.target.name;        const value = event.target.value;        setInputs((values) => ({ ...values, [name]: value }));    };    const handleSubmit = (event: any) => {        event.preventDefault();    };    return (        <form>            <div className={'flex flex-col gap-3'}>                <div className={'flex w-full flex-row gap-3'}>                    <Input                        name={'lastName'}                        value={inputs.lastName || ''}                        placeholder={'Last Name'}                        onChange={handleChange}                    />                    <Input                        name={'firstName'}                        value={inputs.firstName || ''}                        placeholder={'First Name'}                        onChange={handleChange}                    />                </div>                <div className={'flex w-full flex-row'}>                    <Input                        name={'email'}                        value={inputs.email || ''}                        placeholder={'Email'}                        onChange={handleChange}                    />                </div>                <div className={'flex w-full flex-row'}>                    <Input                        name={'phone'}                        value={inputs.phone || ''}                        placeholder={'Mobile'}                        onChange={handleChange}                    />                </div>                <div className={'flex w-full flex-row'}>                    <textarea                        className={                            'h-[130px] w-full rounded-xl  bg-[#d9d9d9] pl-3.5 pt-3.5 font-lato text-black placeholder:font-light placeholder:text-black'                        }                        name={'message'}                        value={inputs.message || ''}                        placeholder={'Message'}                        onChange={handleChange}                        rows={4}                    />                </div>                <button                    className={cn(                        `inline-flex h-[55px] w-full flex-col items-center justify-center gap-2 rounded-lg bg-submit-gradient py-4 border border-white z-10`,                        "group relative flex overflow-hidden rounded-lg border px-8 py-4 after:absolute after:inset-x-0 after:-top-full after:bottom-full after:-z-9 after:block after:bg-primary-red after:px-2 after:py-6 after:transition-all after:duration-300 after:content-[''] hover:after:inset-x-0 after:hover:bottom-0 hover:after:top-0",                    )}                >                    <p                        className={`z-10 font-lato text-lg font-bold text-white transition-all duration-300 group-hover:text-primary-white`}                    >                        SUBMIT                    </p>                </button>            </div>        </form>    );};