import * as React from 'react';import { useAvailabilityContext } from '@/features/availability/context/availability-context';import { Logo } from '@/features/header/components';import { MenuDesktop } from '@/features/header/components/menu-desktop';import { MenuMobile } from '@/features/header/components/menu-mobile';export const Header: React.FC = () => {    const { isModalOpen } = useAvailabilityContext();    return (        <>            {!isModalOpen && (                <div className={'z-40 fixed flex flex-row h-[5.625rem] w-screen justify-center items-center'}>                    <div className=" box-border h-[5.625rem] w-[1500px] overflow-hidden px-8 m">                        <div                            className="box-border flex size-full flex-row items-center justify-between overflow-hidden">                            <Logo />                            <MenuMobile />                            <MenuDesktop />                        </div>                    </div>                </div>            )}        </>    );};