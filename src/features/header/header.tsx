import * as React from 'react';import { Logo } from '@/features/header/components';import { MenuDesktop } from '@/features/header/components/menu-desktop';import { MenuMobile } from '@/features/header/components/menu-mobile';import { useScroll } from '@/hooks/use-scroll';import { cn } from '@/utils/cn';export const Header: React.FC = () => {    const { scrollDirection } = useScroll();    return (        <div            className={cn(                'fixed z-40 flex w-screen flex-row items-center justify-center bg-primary-grey transition-all duration-500',                scrollDirection === 'up' ? 'h-20' : 'h-40',            )}        >            <div className="box-border w-[1440px] overflow-hidden">                <div className="box-border flex size-full flex-row items-center justify-between overflow-hidden">                    <Logo />                    <MenuMobile />                    <MenuDesktop />                </div>            </div>        </div>    );};