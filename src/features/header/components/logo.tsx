import * as React from 'react';import icon from '@/assets/4918776-200.png';import { Link } from '@/components/ui/link';import { useNavigationContext } from '@/context/navigation-context';export const Logo = () => {    const { navigateToPage } = useNavigationContext();    return (        <div className={`w-[200px] md:w-auto`}>            <Link onClick={() => navigateToPage('landing')}>                <span                    className={`flex flex-row items-center gap-2 bg-nav-gradient bg-nav-size bg-left-bottom bg-no-repeat pb-[2px] font-secondaryRegular text-4xl font-normal text-white `}                >                    <img src={icon} className="w-[40px] h-[40px] invert" />                NEPTUNE                </span>            </Link>        </div>    );};