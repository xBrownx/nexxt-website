import * as React from 'react';import { Link } from '@/components/ui/link';import { useNavigationContext } from '@/context/navigation-context';export const Logo = () => {    const { navigateToPage } = useNavigationContext();    return (        <div className={`w-[500px]`}>            <Link onClick={() => navigateToPage('landing')}>                <span                    className={`w-fit items-center bg-nav-gradient bg-nav-size bg-left-bottom bg-no-repeat pb-[2px] font-logo text-5xl font-normal text-navbar-logo `}                >                    CBxDT                </span>            </Link>        </div>    );};