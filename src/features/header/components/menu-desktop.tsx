import * as React from 'react';import { headerConfig, HeaderConfigKey } from '@/config';import { NavButton } from '@/features/header/components/nav-button';import { NavLink } from '@/features/header/components/nav-link';export const MenuDesktop = () => {    return (        <div className="box-border hidden size-full items-center justify-end lg:flex">            <ul className={`flex flex-row items-center gap-8`}>                {Object.keys(headerConfig).map((key) => {                    return (                        <li key={key}>                            {headerConfig[key as HeaderConfigKey].type ===                            'link' ? (                                <NavLink propKey={key} />                            ) : (                                <NavButton propKey={key} />                            )}                        </li>                    );                })}            </ul>        </div>    );};