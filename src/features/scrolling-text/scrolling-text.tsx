import * as React from 'react';import { InfiniteScroll } from '@/components/ui/infitine-scroll';type SplitHeadingProps = {    children: string;};const SplitHeading: React.FC<SplitHeadingProps> = ({ children }) => {    return (        <div className={`flex flex-row`}>            {children.split('').map((item, idx) => (                <h1                    className={`text-center font-primaryLight text-8xl font-light uppercase text-[#84936e]/50 md:text-[300px] md:leading-[300px]`}                    key={idx}                >                    {item === ' ' ? <>&nbsp;</> : item}                </h1>            ))}        </div>    );};export const ScrollingText = () => {    return (        <div            className={`flex w-full flex-row items-center justify-center overflow-hidden bg-background-secondary pb-16 md:h-fit md:pb-0`}        >            <InfiniteScroll>                <SplitHeading>                    140 WILLIAM STREET • 140 WILLIAM STREET •                </SplitHeading>            </InfiniteScroll>        </div>    );};