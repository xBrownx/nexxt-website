import { motion } from 'framer-motion';import * as React from 'react';type ParallaxFadeInProps = {    children: React.ReactNode;};export const ParallaxFadeIn: React.FC<ParallaxFadeInProps> = ({ children }) => {    return (        <motion.div            initial={{ opacity: 0 }}            whileInView={{ opacity: 1 }}            viewport={{                margin: '0px 0px -200px 0px',                amount: 'some',                once: true,            }}            transition={{                duration: 0.5,                ease: 'linear',            }}            className={`size-full`}        >            {children}        </motion.div>    );};