import { cn } from '@/utils/cn';export const GlitchHover = ({    className,    children,}: {    className?: string;    infinite?: boolean;    children: string;}) => {    return (        <div            className={cn(                'group relative h-fit flex-1 font-lato text-5xl font-bold text-white',                className,            )}        >            <span                aria-hidden={true}                className={cn(                    'relative left-0 top-0',                    'group-hover:animate-glitch-text-01-inf',                )}            >                {children}            </span>            <span                className={cn(                    'absolute hidden right-[-1%] bottom-[-1%] opacity-80 [clip-path:polygon(0%_0%,100%_0%,100%_50%,0%_50%)]',                    'group-hover:animate-glitch-text-00-inf  group-hover:inline-block',                )}            >                {children}            </span>            <span                aria-hidden={true}                className={cn(                    'absolute hidden opacity-80 left-[1%] top-[1%] [clip-path:polygon(0%_60%,100%_60%,100%_100%,0%_100%)]',                    'group-hover:animate-glitch-text-02-inf group-hover:inline-block text-nowrap',                )}            >                {children}            </span>        </div>    );};