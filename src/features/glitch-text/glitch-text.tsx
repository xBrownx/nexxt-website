import { cn } from '@/utils/cn';export const GlitchText = ({    className,    children,}: {    className?: string;    infinite?: boolean;    children: string;}) => {    return (        <div            className={cn(                'relative h-fit flex-1 font-title text-5xl font-bold text-white w-fit text-nowrap ',                className,            )}        >            <span                className={cn(                    'relative left-[-0.025em] top-[-0.025em] z-10 animate-glitch-text-00 opacity-80 [clip-path:polygon(0%_0%,100%_0%,100%_50%,0%_50%)] w-fit',                )}            >                {children}            </span>            <span                aria-hidden={true}                className={cn(                    'absolute left-0 top-0 animate-glitch-text-01 w-fit',                )}            >                {children}            </span>            <span                aria-hidden={true}                className={cn(                    ' w-fit absolute left-[0.025em] top-[0.025em] z-10 animate-glitch-text-02 opacity-80 [clip-path:polygon(0%_60%,100%_60%,100%_100%,0%_100%)]',                )}            >                {children}            </span>        </div>    );};