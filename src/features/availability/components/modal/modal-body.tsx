import * as React from 'react';import { availabilityAssets as ASSETS } from '@/assets';import { TenancyConfigKey } from '@/config';import {    Details,    ModalThirdspace,} from '@/features/availability/components/modal/modal-details';import { NavDesktop } from '@/features/availability/components/nav';import { useAvailabilityContext } from '@/features/availability/context/availability-context';const vectorAssets = ASSETS.img['tenancy-vectors'];export const ModalBody = () => {    const { activeLevel } = useAvailabilityContext();    if (!activeLevel) return;    return (        <div className={`flex h-full flex-row items-center gap-16 px-8 py-6`}>            <NavDesktop />            {activeLevel === 'l05' ? (                <ModalThirdspace />            ) : (                <>                    <Details />                    <div>                        <img                            src={                                vectorAssets[activeLevel as TenancyConfigKey]                                    .src                            }                            alt={'level-floor-plan'}                        />                    </div>                </>            )}        </div>    );};