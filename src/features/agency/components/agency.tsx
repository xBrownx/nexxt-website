import * as React from 'react';import { Agent } from '@/features/agency/components/agent';import { AgencyType } from '@/features/agency/types';export const Agency: React.FC<AgencyType> = ({ name, link, agents }) => {    return (        <div className={`flex flex-col gap-4`}>            <a href={link} target="_blank" rel="noopener noreferrer">                <h2                    className={`w-fit border-b border-[#5178e1] py-1 text-center font-secondaryLight text-xl font-normal uppercase leading-tight text-[#5178e1]`}                >                    {name}                </h2>            </a>            {agents.map((agent) => (                <Agent key={agent.id} {...agent} />            ))}        </div>    );};