import { cva, VariantProps } from 'class-variance-authority';import React from 'react';import { ParallaxImage } from '@/components/ui/parallax';import { cn } from '@/utils/cn';const layoutVariants = cva(['h-fit', 'w-screen', 'py-8'], {    variants: {        variant: {            default: 'bg-background-primary',            secondary: 'bg-background-secondary',        },        size: {            padded: 'md:px-16 md:py-32',            fullscreen: 'md:p-0',            mobile: 'h-auto px-4 pb-16 pt-8',            'mobile-full': 'h-auto px-0 pb-16 pt-8',        },    },    defaultVariants: {        variant: 'default',        size: 'padded',    },});type HeroLayoutProps = VariantProps<typeof layoutVariants> & {    img: { src: string; alt: string };};export const HeroLayout: React.FC<HeroLayoutProps> = ({    variant,    size,    img,}) => {    return (        <section className={cn(layoutVariants({ variant, size }))}>            <ParallaxImage                src={img.src}                alt={img.alt}                className={cn(                    `size-full object-cover `,                    size === 'fullscreen' || size === 'mobile-full'                        ? ''                        : 'rounded-lg',                )}            />        </section>    );};