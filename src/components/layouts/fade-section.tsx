import { cva, VariantProps } from 'class-variance-authority';import { motion } from 'motion/react';import * as React from 'react';import { PropsWithChildren } from 'react';import { cn } from '@/utils/cn';const SectionVariants = cva(['overflow-x-hidden', 'box-border', 'w-screen'], {    variants: {        variant: {            default: '',            fadeInPurple: 'bg-accent-purple',            fadeInWhite: 'bg-accent-purple',        },    },    defaultVariants: {        variant: 'default',    },});type SectionProps = PropsWithChildren &    VariantProps<typeof SectionVariants> & {        className?: string;        initColor: string;        enterColor: string;    };export function FadeSection({    variant,    children,    className,    initColor,    enterColor,}: SectionProps) {    return (        <motion.div            className={cn(SectionVariants({ variant }), className)}            initial={{ backgroundColor: initColor }}            whileInView={{ backgroundColor: enterColor }}            exit={{ backgroundColor: '#c4adee' }}            transition={{                delay: 1.5,                duration: 0.5,            }}        >            {children}        </motion.div>    );}