import { motion } from 'framer-motion';import * as React from 'react';type ParallaxTitleProps = {    children: React.ReactNode;};export const ParallaxTitle: React.FC<ParallaxTitleProps> = ({ children }) => {    return (        <div className={`overflow-hidden`}>            <motion.div                className={'h-fit'}                initial={{ y: '100%' }}                whileInView={{ y: 0 }}                viewport={{                    margin: '0px 0px -50px 0px',                    amount: 'some',                    once: true,                }}                transition={{                    duration: 0.2,                    ease: 'linear',                }}            >                {children}            </motion.div>        </div>    );};