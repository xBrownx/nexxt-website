import { motion } from 'framer-motion';import * as React from 'react';type FadeUpParallaxProps = {    children: React.ReactNode;};export const ParallaxFadeUp: React.FC<FadeUpParallaxProps> = ({ children }) => {    return (        <motion.div            initial={{ y: '100%', opacity: 0 }}            whileInView={{ y: 0, opacity: 1 }}            viewport={{                margin: '0px 0px -50px 0px',                amount: 'some',                once: true,            }}            transition={{                duration: 0.2,                ease: 'linear',            }}        >            {children}        </motion.div>    );};