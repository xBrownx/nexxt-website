import { useScroll, useTransform, motion } from 'framer-motion';import * as React from 'react';import { cn } from '@/utils/cn';type ParallaxImageProps = {    src: string;    alt: string;    className?: string;};export const ParallaxImage: React.FC<ParallaxImageProps> = ({    src,    alt,    className,}) => {    const targetRef = React.useRef<HTMLDivElement | null>(null);    const { scrollYProgress } = useScroll({        target: targetRef,        offset: ['start end', 'end start'],    });    const scale = useTransform(scrollYProgress, [0, 0.5], ['200%', '100%']);    return (        <div            ref={targetRef}            className={cn(                `relative box-border flex size-full items-center justify-center overflow-hidden`,                className,            )}        >            <motion.img                src={src}                alt={alt}                style={{ scale: scale }}                className={cn(                    `box-border size-full object-cover overflow`,                    className,                )}            />        </div>    );};