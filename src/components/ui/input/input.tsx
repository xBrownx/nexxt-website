import * as React from 'react';import { ChangeEventHandler } from 'react';type InputProps = React.InputHTMLAttributes<HTMLInputElement>;export const Input: React.FC<InputProps> = ({    name,    value,    placeholder,    onChange,}) => {    return (        <input            className={`flex h-[58px] w-full rounded-xl bg-[#d9d9d9] pl-[20px] font-lato text-black placeholder:font-light placeholder:text-black`}            name={name}            value={value || ''}            placeholder={placeholder}            onChange={onChange}        />    );};